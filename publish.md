# How to publish

```bash
./bin/release.sh X.X.X
# create and merge PR

# create new release on GitHub
# https://github.com/mkobayashime/bookmarklets-cli/releases/new
# create a new tag, then `Generate release notes`
```
